Terms:

- Vesrion control

// Interview.Q:

- git
- github - gitlab - azure

// Interview.Q:
why not use main branch only ????

- Repository (folder)

- Branch (to add new feature - main) :
  registration (reg_branch) ----> merge (main)
  Login (login_branch) ---> merge (main)

- Git clone --repo URL-- : clone the repository

- git add . ----->
  :بنقل الشغل بتاعي من
  Local repository to Working area

- git commit -m "الرسالة بتاعتك"
  بتنقل الشغل بتاعك من
  Working area to staging area

- git push
  بتنقل الشغل بتاعي من
  Staging area to Remote Repository

- git pull

بمراااااحل ال traking

---

- git branch (branch name) ----> to create new branch from the current branch

- git chechout (branch name) ----> to move to another branch

- git checkout -b (branch name) -----> create new branch and move to it at the same time

////////

////
new data
